import 'package:flutter_application_eval/home.dart';
import 'package:flutter/material.dart';

const home = '/home';
const details = '/details';

Map<String, WidgetBuilder> routes = {
  home: (context) => const MyHomePage(),
};
